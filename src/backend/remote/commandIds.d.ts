export const enum CommandId {
  // 1-99 Custom / electron / test
  test_trigger_error = 9,

  /* 100-149 DC without context */
  //   getInfoWithoutContext = 100,
  openContext = 101,

  // 150-up DC with Context
  getInfo = 150,
}
